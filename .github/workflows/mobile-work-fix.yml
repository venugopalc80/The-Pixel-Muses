name: Apply mobile work layout fix

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  fix:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary workflow disabled"
