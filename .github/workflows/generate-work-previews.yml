name: Build Our Work showcase
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  showcase:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Our Work showcase build runner is active"
